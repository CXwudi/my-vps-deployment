services:
  app:
    ports:
      - "${LOBECHAT_HOST_PORT:-3210}:3210"
    environment:
      APP_URL: ${APP_URL:-http://localhost:3210}

  search:
    ports:
      - "${SEARXNG_HOST_PORT:-8080}:8080"
