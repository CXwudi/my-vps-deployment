# Lego Notes

Starting in 2023, running lego from the local environment stopped working. It
might be related to the OpenWrt and AdGuard Home setup, so renewals are kept as
VPS-side operations.

The account data under `./lego-data/accounts/` must be preserved because lego
needs the same ACME account for renewal.
