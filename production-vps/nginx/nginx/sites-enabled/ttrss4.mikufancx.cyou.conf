server {
    listen              443 ssl;
    listen              [::]:443 ssl;
    http2               on;
    server_name         ttrss4.mikufancx.cyou;

    # SSL
    ssl_certificate     /etc/ssl/_.mikufancx.cyou.crt;
    ssl_certificate_key /etc/ssl/_.mikufancx.cyou.key;

    # security
    include             nginxconfig.io/security.conf;

    # logging
    access_log          /var/log/nginx/ttrss4.mikufancx.cyou.access.log cloudflare buffer=512k flush=1m;
    error_log           /var/log/nginx/ttrss4.mikufancx.cyou.error.log warn;

    # the patch
    include             my-patches/ttrss4-patch.conf;

    # reverse proxy
    location / {
        resolver              127.0.0.11 [::1]:5353 valid=15s;
        set                   $backend "http://fake-site-entry:81";
        proxy_pass            $backend;
        include               nginxconfig.io/proxy/standard_proxy.conf;
    }

    # additional config
    include nginxconfig.io/general.conf;
}

# HTTP redirect
server {
    listen      80;
    listen      [::]:80;
    server_name ttrss4.mikufancx.cyou;
    return      301 https://ttrss4.mikufancx.cyou$request_uri;
}